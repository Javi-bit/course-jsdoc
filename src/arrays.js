/**
 * @module Arrays
 */

/**
 * Arrays of age
 * @type {Array}
 */
const age = [ 21, 22, 75, 90, 43, "asdga", {} ]

/**
 * Arrays of names
 * @type {Array<string>}
 */
const names = [ "Ruth", "john", "Sophia", "Don" ]

/**
 * Arrays of names
 * @type {Array<number | string>}
 */
const number_strings = [ "Ruth", 2, 87, "Don" ]